Fixed document viewer broken on Android 11+ (missing queries + no auth). Documents now download locally and open via open_filex. Onboarding shift time pickers now propagate selected times to server. FCM notification tap now navigates to correct screen. FCM token removed on logout. Fixed LedgerService crash, PayrollEntry.toJson missing photoUrl, DailySummaryData missing date, Admin role security (allowlist), Staff update phone validation, ExportCSV corrupt responses, Settings error leaking. Added DocumentService.downloadDocument, Dispute.toJson, sync queue RBAC, dispute ownership check.
