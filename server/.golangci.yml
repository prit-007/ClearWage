linters-settings:
  errcheck:
    check-blank: true
  misspell:
    locale: US
  govet:
    enable:
      - shadow
  gosimple:
    checks: ["all"]

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - misspell

run:
  timeout: 5m
  tests: true
