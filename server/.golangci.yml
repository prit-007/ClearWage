version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell
  settings:
    errcheck:
      check-blank: true
    misspell:
      locale: US
    govet:
      enable:
        - shadow

run:
  timeout: 5m
  tests: true
