version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell
  settings:
    errcheck:
      check-blank: false
    misspell:
      locale: US
    govet:
      enable:
        - shadow

run:
  timeout: 5m
  tests: true
